import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env' }); const supabaseAdmin = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data, error } = await supabaseAdmin.rpc('get_tables'); if (error) { // If RPC doesn't exist, try direct query to pg_catalog const { data: d2, error: e2 } = await supabaseAdmin.from('pg_tables').select('tablename').eq('schemaname', 'public'); console.log('Direct query:', d2, 'Error:', e2); } else { console.log('Tables:', data); } } run();
